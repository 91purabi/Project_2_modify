import 'package:flutter/material.dart';

class ProjectBudgetCardVerticalDividerWidget extends StatelessWidget {
  const ProjectBudgetCardVerticalDividerWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return VerticalDivider(
      color: Colors.white,
      thickness: 2,
    );
  }
}
